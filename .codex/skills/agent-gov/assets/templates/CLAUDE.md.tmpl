# Claude Project Memory

Use the shared project agent instructions:

@AGENTS.md

Keep long-running development state in `.agent/sessions/`.
Use `.agent/context.json` to keep loaded governance context bounded.
