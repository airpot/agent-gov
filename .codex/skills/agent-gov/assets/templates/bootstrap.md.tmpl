# Session Bootstrap

No active session yet.

Start one with:

```bash
python3 .agent/tools/agent_session.py start <name> --goal "<goal>"
```

If a session exists, refresh this file with:

```bash
python3 .agent/tools/agent_session.py bootstrap
```

