interface:
  display_name: "Spec Archive"
  short_description: "Archive completed embedded spec changes"
  default_prompt: "Use $openspec-archive-change to finalize and archive a completed embedded spec change."
